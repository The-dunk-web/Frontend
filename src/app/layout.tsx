import type { Metadata } from 'next';
import { Inter, Orbitron, Press_Start_2P, Oswald, VT323 } from 'next/font/google';
import './globals.css';

export const metadata: Metadata = {
  title: 'The Dunk Web',
  description: 'Generated by create next app',
};
const inter = Inter({ subsets: ['latin'] });
const oswald = Oswald();

const orbitron = Orbitron();
const press_start_2p = Press_Start_2P({ weight: '400' });
const vt323 = VT323({ weight: '400' });

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${orbitron.className} bg-stone-950 font-semibold tracking-wider text-stone-100 antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
